<!DOCTYPE html>
<html lang="en">
<head>
    <%- include ('./partials/header.ejs') %>
    <link rel="stylesheet" href="/styles/movies.css">
</head>
<body>
    <img src="<%=movies.poster%>" id="moviebg">
    <h1 id="homebutton"><a href="/">RecommendMe</a></h1>
    <h2 id="backbutton"><a href="/movies/">Back to Movies</a></h2>
    <div class="row">
        <div class="col-sm-12">
            <h2><%=movies.title%></h2>
            <h3><%=movies.director%></h3>
            <h4>Release Date: <%=movies.release_date%></h4>
            <h5><%=movies.review%></h5>
            <h4>
                <form action="/movies/<%= movies._id %>/like?_method=PUT" method="POST">
                    <button type="submit"><%=movies.likes%> 
                        <% if (movies.likes === 1) { %>
                            Like
                       <% } else { %>
                            Likes
                       <% } %>
                    </button></form>
                
            </h4>

            <br>
            <h5><a href="/movies/<%= movies._id %>/edit"><button>Edit Movie</button></a></h5>
            
            <h5><form action="/movies/<%= movies._id %>?_method=DELETE" method="POST">
                <button type="submit">Delete Movie</button>
            </form></h5>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
</body>
</html>